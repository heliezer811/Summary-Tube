pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        // maven { url 'https://jitpack.io' } // Repositório para o Markdown
    }
}
rootProject.name = "Summary-Tube"
include ':app'
